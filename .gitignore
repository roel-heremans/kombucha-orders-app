# OS / editor cruft
.DS_Store
Thumbs.db
*~
.*.swp
.vscode/
.idea/

# Dependencies / build (not used, but ignored just in case)
node_modules/
dist/
*.log

# Local Firebase config overrides (if ever used)
firebase-config.local.js
